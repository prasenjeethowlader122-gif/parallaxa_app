name: Shorebird OTA Update

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  patch:
    name: Shorebird Patch
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter          # ← ADD THIS
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Shorebird
        uses: shorebirdtech/setup-shorebird@v1

      - name: Install dependencies
        working-directory: artifacts/social-flutter-app
        run: flutter pub get         # ← was: shorebird flutter pub get

      - name: Generate code (build_runner)
        working-directory: artifacts/social-flutter-app
        run: flutter pub run build_runner build --delete-conflicting-outputs  # ← same fix
      - name: Shorebird Patch
        working-directory: artifacts/social-flutter-app
        run: shorebird patch android --allow-native-diffs --allow-asset-diffs
        env:
          SHOREBIRD_TOKEN: sb_api_h_hoa9KjK0CNm0J5FxiwlFLV87jHjSTkLPgH4DIPTDg